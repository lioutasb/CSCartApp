<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item android:id="@+id/action_cart"
        android:icon="@drawable/ic_action_cart_white"
        android:title="@string/menu_cart_name"
        android:actionLayout="@layout/layout_notification_cart_icon"
        app:showAsAction="always"/>

    <item android:id="@+id/action_share"
        android:icon="@drawable/ic_action_share_white"
        android:title="@string/menu_share_name"
        android:visible="false"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_search"
        android:icon="@drawable/ic_action_search_white"
        android:title="@string/menu_search_name"
        app:showAsAction="never"/>

    <item android:id="@+id/action_settings"
        android:title="@string/menu_settings_name"
        app:showAsAction="never" />
</menu>
